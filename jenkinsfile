pipeline { 
    agent any  
    stages { 
        stage('Build') { 
            steps { 
               echo 'This is a pipeline code' 
            }
        }
	stage('Linux_Command') {
	    steps {
                sh mkdir /tmp/pipeline
	    }
	}
    }
}
