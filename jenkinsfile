pipeline { 
    agent any  
    stages { 
        stage('Build') { 
            steps { 
               echo 'This is a pipeline code' 
            }
        }
	stage("Linux_Command") {
	    mkdir /tmp/pipeline
	}
    }
}
