pipeline { 
    agent any  
    stages { 
        stage('Build') { 
            steps { 
               echo 'This is a pipeline code' 
            }
        }
	stage("Linux command") {
	    mkdir /tmp/pipeline
	    chown root:root /tmp/pipeline
	}
    }
}
